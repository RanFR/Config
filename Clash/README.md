# How to use

First initialize the workspace.

```bash
npm init -y
```

Then install `js-yaml` package.

```bash
npm install js-yaml
```

To debug, use command `node Debug.js`.

## Match 规则检测

程序位于文件夹`log_analyzer`下，用法参考[详细说明](docs/Log-Analyzer.md)。
